/**
 * ExtensionPay extension ID. Sign up at https://extensionpay.com and register
 * your extension to get an ID (e.g. "cookiecontrol"). Use that here.
 */
(function (g) {
  g.EXTPAY_EXTENSION_ID = 'phgncgdjadpmeoolimiiiklkmcohldgi';
})(typeof self !== 'undefined' ? self : typeof window !== 'undefined' ? window : this);
